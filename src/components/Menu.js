import '../App.css';

const Menu = () => {
  return (
    <div className="Menu">
      <label>Enter Your Name</label>
      <input type="text" placeholder="Ex." />
      <button>Start Quiz</button>
    </div>
  );
};

export default Menu;
